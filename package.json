{
  "name": "yt-rag",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "discover": "pnpm --filter @yt-rag/ingest discover",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "ingest": "pnpm --filter @yt-rag/ingest ingest",
    "grep": "pnpm --filter @yt-rag/ingest grep"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
